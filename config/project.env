# Project-wide defaults (sourced by bash via code/lib/bootstrap.sh)

# Root folders (can be overridden per-machine)
LOG_ROOT="${PROJECT_ROOT}/logs"
WORK_ROOT="${PROJECT_ROOT}/work"
BIDS_ROOT="${PROJECT_ROOT}/bids"
DERIV_ROOT="${PROJECT_ROOT}/derivatives"
STAGE_ROOT="${PROJECT_ROOT}/stage"

# Container location + runtime
CONTAINERS_ROOT="${HOME}/containers"
SING_BIN="${SING_BIN:-singularity}"

# Default parallelism (pipelines can override)
N_JOBS_DEFAULT="${N_JOBS_DEFAULT:-4}"

# Default queue mode (can be overridden per-run or per-pipeline)
QUEUE_MODE_DEFAULT="${QUEUE_MODE_DEFAULT:-pending}"
